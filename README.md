Installation instructions:

- Recommend using a virtual environment
- Install required packages using `pip install -r requirements.txt`
- Install MuJoCo with `pip install gymnasium[mujoco]`
- run `python main.py`
